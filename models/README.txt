This directory is intentionally kept as a model artifact placeholder.

Large checkpoint files (.pth) are distributed via GitHub Releases and referenced in:
- ../large_files.yml

Threshold files used during inference are stored under:
- ../weights/fold_*/thresholds.json
